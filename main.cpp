#include <iostream>

int main()
{
    //load conf
    //open listener sock
    //inject listener sock to async main event loop
    //log all event which appears in event loop
    //TODO: async event loop
    return EXIT_SUCCESS;
}
